# RMS Python dependencies (M1 dataset EDA).
# Versions kept compatible with Python 3.10 and with the later mlagents 1.1.0 milestone
# (numpy pinned < 2.0 because ML-Agents / older PyTorch require it — keeps one env for the
# whole project instead of a split M1-vs-training environment).

numpy>=1.23,<2.0
pandas>=2.0,<2.2
scipy>=1.10,<1.14
matplotlib>=3.7,<3.9
seaborn>=0.12,<0.14

# Notebook + tests
jupyter>=1.0
pytest>=7.4,<9.0
