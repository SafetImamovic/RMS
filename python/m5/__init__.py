"""M5: the evaluation and comparison that closes the project.

RL against BC against the scripted driver against the human dataset, on the axes `DESIGN.md` 7 and
7.1 name, with the two distortions those sections warn about handled rather than ignored.
"""
